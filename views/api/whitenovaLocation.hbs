{{ info.login }}'s 15 days period: (Time zone: <define>{{ info.timezone }}</define>)
  - Start: <string>{{ info.start_at }}</string>
  - End: <string>{{ info.end_at }}</string>

{{#each data as |l|}}
<string>{{ l.begin_at }}</string> -- <string>{{ l.end_at }}</string> -> <number>{{ l.duration }}</number> {{ l.host_name }}
{{/each}}